import React from 'react';
import {Head} from '../Head';
import NotFound from '../NotFound';

export const NotFoundPage = () => {
  return (
    <React.Fragment>
      <Head />
      <NotFound />
    </React.Fragment>
  );
};
