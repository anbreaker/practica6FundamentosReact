import React from 'react';
import Layout from '../Layout';
import FormChangeAdvert from '../FormChangeAdvert';

function FormChangeAdvertPage() {
  return (
    <Layout>
      <FormChangeAdvert uploadImage />
    </Layout>
  );
}

export default FormChangeAdvertPage;
