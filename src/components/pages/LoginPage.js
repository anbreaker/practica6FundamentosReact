import React from 'react';
import Login from '../Login';
import Layout from '../Layout';

function LoginPage() {
  return (
    <Layout>
      <Login />
    </Layout>
  );
}

export default LoginPage;
